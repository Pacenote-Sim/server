// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: stats.sql

package gen

import (
	"context"
)

const countActiveDevices = `-- name: CountActiveDevices :one
SELECT count(*) AS count FROM devices WHERE revoked_at IS NULL
`

func (q *Queries) CountActiveDevices(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countActiveDevices)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countDrivers = `-- name: CountDrivers :one
SELECT count(*) AS count FROM drivers
`

func (q *Queries) CountDrivers(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countDrivers)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countLaps = `-- name: CountLaps :one
SELECT count(*) AS count FROM laps
`

func (q *Queries) CountLaps(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countLaps)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countStints = `-- name: CountStints :one
SELECT count(*) AS count FROM stints
`

func (q *Queries) CountStints(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countStints)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const databaseSizeBytes = `-- name: DatabaseSizeBytes :one

SELECT pg_database_size(current_database())::bigint AS bytes
`

// What the admin panel's overview page shows. Every one of these is a single
// statement, because the page is loaded by someone waiting for it.
func (q *Queries) DatabaseSizeBytes(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, databaseSizeBytes)
	var bytes int64
	err := row.Scan(&bytes)
	return bytes, err
}

const traceBytes = `-- name: TraceBytes :one
SELECT coalesce(sum(length(trace)), 0)::bigint AS bytes FROM laps
`

func (q *Queries) TraceBytes(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, traceBytes)
	var bytes int64
	err := row.Scan(&bytes)
	return bytes, err
}
