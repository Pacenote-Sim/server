// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: clients.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const clientBuildByReference = `-- name: ClientBuildByReference :one
SELECT id, at, actor, address, signing, reference, file_name, file_bytes, sha256, client_version, signed_by FROM client_builds WHERE reference = $1
`

func (q *Queries) ClientBuildByReference(ctx context.Context, reference string) (ClientBuild, error) {
	row := q.db.QueryRow(ctx, clientBuildByReference, reference)
	var i ClientBuild
	err := row.Scan(
		&i.ID,
		&i.At,
		&i.Actor,
		&i.Address,
		&i.Signing,
		&i.Reference,
		&i.FileName,
		&i.FileBytes,
		&i.Sha256,
		&i.ClientVersion,
		&i.SignedBy,
	)
	return i, err
}

const clientBuildsPage = `-- name: ClientBuildsPage :many
SELECT id, at, actor, address, signing, reference, file_name, file_bytes, sha256, client_version, signed_by FROM client_builds
 WHERE $1::boolean
    OR (at, id) < ($2::timestamptz, $3::bigint)
 ORDER BY at DESC, id DESC
 LIMIT $4
`

type ClientBuildsPageParams struct {
	First   bool
	AfterAt pgtype.Timestamptz
	AfterID int64
	Lim     int32
}

func (q *Queries) ClientBuildsPage(ctx context.Context, arg ClientBuildsPageParams) ([]ClientBuild, error) {
	rows, err := q.db.Query(ctx, clientBuildsPage,
		arg.First,
		arg.AfterAt,
		arg.AfterID,
		arg.Lim,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ClientBuild{}
	for rows.Next() {
		var i ClientBuild
		if err := rows.Scan(
			&i.ID,
			&i.At,
			&i.Actor,
			&i.Address,
			&i.Signing,
			&i.Reference,
			&i.FileName,
			&i.FileBytes,
			&i.Sha256,
			&i.ClientVersion,
			&i.SignedBy,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const countClientBuilds = `-- name: CountClientBuilds :one
SELECT count(*)::bigint FROM client_builds
`

func (q *Queries) CountClientBuilds(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countClientBuilds)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const insertClientBuild = `-- name: InsertClientBuild :one

INSERT INTO client_builds (actor, address, signing, reference, file_name, file_bytes, sha256, client_version, signed_by)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
RETURNING id, at, actor, address, signing, reference, file_name, file_bytes, sha256, client_version, signed_by
`

type InsertClientBuildParams struct {
	Actor         string
	Address       string
	Signing       string
	Reference     string
	FileName      string
	FileBytes     int64
	Sha256        string
	ClientVersion string
	SignedBy      string
}

// The build history. Keyset pagination, newest first, over the index the
// migration creates: page fifty costs what page one costs, and a build made
// while an operator is reading cannot shuffle a row onto two pages or off
// both.
func (q *Queries) InsertClientBuild(ctx context.Context, arg InsertClientBuildParams) (ClientBuild, error) {
	row := q.db.QueryRow(ctx, insertClientBuild,
		arg.Actor,
		arg.Address,
		arg.Signing,
		arg.Reference,
		arg.FileName,
		arg.FileBytes,
		arg.Sha256,
		arg.ClientVersion,
		arg.SignedBy,
	)
	var i ClientBuild
	err := row.Scan(
		&i.ID,
		&i.At,
		&i.Actor,
		&i.Address,
		&i.Signing,
		&i.Reference,
		&i.FileName,
		&i.FileBytes,
		&i.Sha256,
		&i.ClientVersion,
		&i.SignedBy,
	)
	return i, err
}
