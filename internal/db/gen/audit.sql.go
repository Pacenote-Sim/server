// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: audit.sql

package gen

import (
	"context"
)

const auditEntriesForAction = `-- name: AuditEntriesForAction :many
SELECT id, at, actor, action, subject, detail
FROM audit_log WHERE action = $1 ORDER BY at DESC, id DESC LIMIT $2
`

type AuditEntriesForActionParams struct {
	Action string
	Limit  int32
}

func (q *Queries) AuditEntriesForAction(ctx context.Context, arg AuditEntriesForActionParams) ([]AuditLog, error) {
	rows, err := q.db.Query(ctx, auditEntriesForAction, arg.Action, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []AuditLog{}
	for rows.Next() {
		var i AuditLog
		if err := rows.Scan(
			&i.ID,
			&i.At,
			&i.Actor,
			&i.Action,
			&i.Subject,
			&i.Detail,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const insertAuditEntry = `-- name: InsertAuditEntry :one

INSERT INTO audit_log (actor, action, subject, detail)
VALUES ($1, $2, $3, $4)
RETURNING id, at, actor, action, subject, detail
`

type InsertAuditEntryParams struct {
	Actor   string
	Action  string
	Subject string
	Detail  []byte
}

// The audit trail. Every change an administrator makes in the panel writes one
// row here: who, what, and when. The detail column names the fields that
// changed and never carries their values, because a settings change can be a
// change to an API key.
func (q *Queries) InsertAuditEntry(ctx context.Context, arg InsertAuditEntryParams) (AuditLog, error) {
	row := q.db.QueryRow(ctx, insertAuditEntry,
		arg.Actor,
		arg.Action,
		arg.Subject,
		arg.Detail,
	)
	var i AuditLog
	err := row.Scan(
		&i.ID,
		&i.At,
		&i.Actor,
		&i.Action,
		&i.Subject,
		&i.Detail,
	)
	return i, err
}

const recentAuditEntries = `-- name: RecentAuditEntries :many
SELECT id, at, actor, action, subject, detail
FROM audit_log ORDER BY at DESC, id DESC LIMIT $1
`

func (q *Queries) RecentAuditEntries(ctx context.Context, limit int32) ([]AuditLog, error) {
	rows, err := q.db.Query(ctx, recentAuditEntries, limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []AuditLog{}
	for rows.Next() {
		var i AuditLog
		if err := rows.Scan(
			&i.ID,
			&i.At,
			&i.Actor,
			&i.Action,
			&i.Subject,
			&i.Detail,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
