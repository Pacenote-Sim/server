# build outputs
/dist/
/bin/
/pacenote-server
/pacenote-server.exe
*.exe
*.test
*.out
*.prof
coverage.out

# a running server's data directory, if one is created here during development
/pacenote-data/

# tooling
/.idea/
/.vscode/
*.swp
.DS_Store

# local env
.env
.env.*

# Somewhere to run a client against a development server: its log, its queue
# and its token. None of it is the server's.
/try/
